# Pinned dependency versions for the AutoPYara ACSAC artifact.
#
# WHY THIS FILE EXISTS
#     setup.py declares loose constraints (e.g. "scikit-learn>=1.0") so that
#     ordinary users get current releases. That is right for a published
#     package but wrong for an artifact: a reviewer installing months after
#     submission would resolve different versions than we verified against.
#     These pins are the exact versions the claims were verified with.
#
# HOW TO USE
#     install.sh does NOT use this file by default -- it installs the package
#     normally, which is the path a real user takes and therefore the more
#     honest thing to evaluate.
#
#     Use these pins if you hit a dependency-resolution problem, or to
#     reproduce our environment exactly:
#
#         pip install -r artifact/requirements-lock.txt
#         pip install --no-deps <repo root>
#
#     The provided Dockerfile uses these pins.
#
# VERIFIED ON
#     Ubuntu Linux x86-64, OpenJDK 21, CPython 3.9-3.12
#     (CI additionally verifies 3.9, 3.10, 3.11 and 3.12 against current
#      releases rather than these pins, so both paths are covered.)

jpype1==1.6.0
yara-python==4.5.4
yaramod==4.5.2
scikit-learn==1.8.0
numpy==1.26.4
packaging==26.3
ppdeep==20251115

# Only needed to run the package's own unit tests, not the claims.
pytest==8.3.4
