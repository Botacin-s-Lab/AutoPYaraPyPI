Expected output - Claim 3
================================================================================

SUCCESS CRITERION
    The script exits 0 and the final line reads:

        CLAIM 3: PASS -- the augmented pipeline ran and derived its own cluster count

    All seven checks must report [ok]. The decisive ones are:

        "a cluster count was derived automatically"
            proves the ssdeep + DBSCAN -> predictor-label -> CRDEST path ran,
            since no k_cluster was supplied.

        "k_cluster is rejected with this preset"
            proves the auto-K path is genuinely in control rather than being
            silently overridden.

    On the supplied proxy corpus, which has two families by construction, the
    derived k_clusters was 2 in every run we performed - i.e. the pipeline
    recovered the ground-truth family count without being told it.

THIS CLAIM IS DELIBERATELY NOT BIT-REPRODUCIBLE
    Run it twice and expect the rule to DIFFER. The CRDEST centroid estimator
    partitions its input with an unseeded random number generator, and the seed
    cannot be fixed through the public API in this release.

    Between runs, these may change:
        - the chosen gram_size (we observed 16 and 128 on identical input)
        - the number, content and ordering of hex strings
        - the condition expression

    The derived k_clusters was stable at 2 across our runs, but that is an
    observation, not a guarantee.

    Do NOT diff your output against the transcript below. The checks assert
    structural properties precisely because equality checks would be wrong
    here. See ../../use.txt.

REFERENCE TRANSCRIPT (abridged; progress bars, threshold-search iterations
and JVM chatter removed)
--------------------------------------------------------------------------------
================================================================================
CLAIM 3 - The augmented AutoPYara preset runs and determines its own k
================================================================================

Building the synthetic proxy corpus...
Wrote 6 synthetic samples (2 families x 3) to /tmp/.../corpus

Running the augmented preset with NO k_cluster supplied
(ssdeep + DBSCAN -> predictor labels -> CRDEST centroids; a few minutes)...
[*] Optimizing Threshold for Target K=2
[*] Final Opt: Threshold=50.00, K=2
[*] Clustering 6 files with threshold 50...

Verifying the result:
  [ok]   generate() returned a result -- 14 keys returned
  [ok]   a cluster count was derived automatically -- auto-derived k_clusters=2 (no k_cluster was supplied)
  [ok]   result contains the documented keys -- file_count=6, gram_size=128
  [ok]   rule_string is non-empty -- 2216 chars, 16 lines
  [ok]   rule is structurally well-formed YARA -- 16 hex string(s) declared
  [ok]   rule compiles as real YARA -- accepted by the yara-python compiler
  [ok]   k_cluster is rejected with this preset -- ValueError raised, as documented

--------------------------------------------------------------------------------
Generated rule (first 15 lines):
--------------------------------------------------------------------------------
  rule acsac_claim3
  {
  	//Input TP Rate:
  	//6/6
  	strings:
  		//Benign FP est: -0.0 Malicious FP est: -0.0 Entropy: 6.74 Found in 2 files
  		$x0 = { ... }
  		[... further $xN entries ...]

  		condition:
  (... of (...)) or (... of (...))}

================================================================================
CLAIM 3: PASS -- the augmented pipeline ran and derived its own cluster count
================================================================================
--------------------------------------------------------------------------------

READING THE THRESHOLD SEARCH OUTPUT
    "Optimizing Threshold for Target K=2 ... Final Opt: Threshold=50.00, K=2"
    is the ssdeep/DBSCAN stage searching for a similarity threshold that
    separates the samples into families. On this corpus it converges on the
    correct count.

    You can inspect the fuzzy-hash structure the stage is working from:

        python3 ../../artifact/make_proxy_corpus.py --out /tmp/c --report

    which prints the pairwise ssdeep similarity matrix. On the supplied corpus,
    same-family pairs score in the 60s-70s and cross-family pairs score 0, so
    the families are cleanly separable by fuzzy hashing.

THESE NUMBERS ARE NOT A BENCHMARK
    Claim 2 and Claim 3 exercise the two pipelines the tool offers. The figures
    they print - TP rate, cluster count, gram size, string count - MUST NOT be
    read as a comparison between them.

    The input is a synthetic corpus we constructed. On it, both pipelines
    saturate: across repeated runs both reached a 6/6 TP rate every time. A
    benchmark on which both methods score perfectly cannot distinguish them,
    and a synthetic corpus cannot support a claim about either method's
    behaviour on real malware in any case.

    Any comparative claim between the two approaches rests on the paper's
    evaluation against its real malware corpus, not on this artifact.

TROUBLESHOOTING
    "a cluster count was derived automatically" fails with k_clusters None
        The augmented path did not run. Confirm preset="AutoPYara" is spelled
        exactly and that Claim 1 passes.

    The process is killed partway through
        Insufficient RAM -- see ../../../infrastructure/constraints.txt.
